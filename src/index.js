// const express = require("express");
// const app = express();

// app.use(express.json())
// app.use(express.urlencoded({extended: true}))

// app.use("/", require("https://archive.org/download/Detour_movie/Detour_512kb.mp4"))

// app.listen()
